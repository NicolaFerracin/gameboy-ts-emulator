// TS do not have native 4/8/16 bit types, so we create a wrapper to indicate what we are working with
export type u4 = number;
export type u8 = number;
export type u16 = number;

export type u8RegName = "A" | "B" | "C" | "D" | "E" | "F" | "H" | "L";
